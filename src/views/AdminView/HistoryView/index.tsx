import React from "react";
import PointsHistoryContainer from "../../../containers/PointsHistoryContainer";

const History = () => {
	return (
		<div className="container mt-2 mb-2">
			Historial de puntos
			<PointsHistoryContainer />
		</div>
	);
};

export default History;
